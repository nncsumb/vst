<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.VST"
        tools:targetApi="31">
        <activity android:name="nathan.csumb.vst.LoginActivity" />
        <activity android:name="nathan.csumb.vst.RegisterActivity" />
        <activity android:name="nathan.csumb.vst.LandingPageActivity" />
        <activity android:name="nathan.csumb.vst.SettingsActivity" />
        <activity android:name="nathan.csumb.vst.UserDeleteActivity" />
        <activity android:name="nathan.csumb.vst.AddVitaminActivity" />
        <activity android:name="nathan.csumb.vst.EditVitaminActivity" />
        <activity android:name="nathan.csumb.vst.AdminActivity" />
        <activity
            android:name="nathan.csumb.vst.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>